# PhysMeta-VLM-inference-link 全面审查报告

> 审查对象：`https://github.com/maxhaoyu/physmeta-VLM-inference-link`（main @ 61f4a73）
> 审查范围：16 个文件、约 2388 行（3 个 Python 核心 + 6 份文档 + 配置/Docker/SQL）
> 结论摘要：**架构清晰、代码质量高，无致命 bug；但存在 4 个安全问题、3 个正确性/健壮性问题、2 个文档矛盾，以及若干可维护性改进项。**

---

## 〇、修复记录（2026-09-26，本轮已落地）

第二轮审查（用户补充的高优先级问题）已全部修复，并新增了测试。已修复项对照：

| 问题 | 状态 | 修复位置 |
|---|---|---|
| 状态机竞态（可重复执行/覆盖终态） | ✅ 已修 | `server.py` 新增 `transition()` 带状态条件原子更新；progress/result/fail 仅允许 claimed/processing → 终态 |
| processing 任务不回收 | ✅ 已修 | `_reap_stale_claimed` 扩展为同时回收 claimed + processing（新增 `heartbeat_at` 字段 + `PROCESSING_TIMEOUT_SECONDS`） |
| 结果上传无大小限制 | ✅ 已修 | `_handle_result` 加 `MAX_RESULT_BYTES` 上限 + zip 炸弹防护（文件数/解压体积）+ 临时文件原子写盘 |
| 结果 SHA-256 可选 | ✅ 已修 | 强制校验，缺失/错误一律 400 |
| 节点端未捕获普通异常 | ✅ 已修 | `agent.py` 单任务边界捕获 `Exception` + traceback + 保证 `/fail`（`AgentAuthenticationError` 仍上抛） |
| 认证配置缺失不拒绝 | ✅ 已修 | `main()` 启动强制校验两个 token ≥32 字符，否则 `SystemExit` |
| 状态查询未鉴权 | ✅ 已修 | `_handle_status` 加 `UPLOAD_TOKEN` 校验 |
| Content-Length 非法抛 500 | ✅ 已修 | `_read_body` 容错解析 + 循环读满 |
| `wait_seconds` 0 值被 `or 5` 吞掉 | ✅ 已修 | 改为显式 `float` 转换，0 保留 |
| `bool("false")` 误判 | ✅ 已修 | 新增 `_to_bool()` 安全布尔解析 |
| VLM 旧模型未释放 | ✅ 已修 | `_load` 切换模型前 `del` + `empty_cache` |
| LoRA 静默降级 | ✅ 已修 | 降级时打印 `[警告]` 显式告警 |
| 域名漂移（app vs yolobubble） | ✅ 已修 | example.json 与 `load_config` 默认值统一为 `yolobubble.physmeta.cn` |
| `.gitignore` 漏模型规则 | ✅ 已修 | 补 `*.safetensors`/`*.pt`/`models/`/`adapters/` 等 |
| 依赖未锁定 | ✅ 已修 | `requirements-windows.txt` 收敛上下界；transformers 锁 `<5.17.0` 规避已知崩溃 |
| 磁盘清理/保留期 | ✅ 已修 | 新增 `_cleanup_expired()` + 后台清理线程 + `RETENTION_SECONDS` |
| 无测试 | ✅ 已补 | `server/tests/test_server.py`（11 用例）+ `node/tests/test_agent.py`（5 用例），全部通过 |

**尚未处理（建议后续跟进，不阻塞当前）**：ThreadingHTTPServer 请求级限流（本轮只加了 `daemon_threads`，未做信号量限流）、CI/依赖审计、LICENSE、节点 fake pipeline 集成测试（需 bubble-ocr 环境，无法在无 GPU 沙箱内跑）。

---

## 一、总体评价（先说结论）

这是一个"最小化推理链路"：服务器端（`server/server.py`）+ Windows 节点端（`node/agent.py`）+ VLM 后端（`node/vlm_fallback_windows.py`）。核心设计是"节点主动出站长轮询领单"，天然穿透客户 NAT，守住"图纸不出内网"红线。

**优点（做得好的地方）：**
- 服务器端用纯标准库（`http.server` + `sqlite3`），零第三方依赖，与平台其余服务（identity/dxf/ocr）技术栈完全对齐。
- token 用 `hmac.compare_digest` 常量时间比较，防时序侧信道。
- 领单用 `BEGIN IMMEDIATE` 写锁保证"查 pending + 标记 claimed"原子性，避免多节点并发重复领取。
- 下载有 `.part` 临时文件 + 字节数 + SHA-256 双重校验；回传有 `X-Artifact-SHA256` 校验。
- claimed 任务超时回收（`CLAIM_TIMEOUT_SECONDS`），节点崩溃不会永久卡死任务。
- VLM 后端做了大量务实的加速（bf16 + SDPA + LoRA merge_and_unload + inference_mode），并有成熟的降级链（SDPA→eager、LoRA→裸模型、VLM缺失→纯OCR）。
- 文档非常详细，DEPLOY.md 是自包含的部署指令，红线意识强（图纸不出内网、密钥不入 git）。

**一句话：这套代码是"能用、能上线"的水平，不是草稿。下面列的问题都是"上线后可能咬人"的隐患，而非"跑不起来"的错误。**

---

## 二、安全问题（按严重度排序，建议优先修）

### 🔴 S1. `GET /api/inference/jobs/{id}`（状态查询）无任何鉴权

`server.py:454-466` 的 `_handle_status` **完全没有任何认证**。任何人只要知道/猜到 job_id，就能查询任务状态。

- job_id 形如 `cad-20260923-132500-` + 6 位十六进制（`secrets.token_hex(3)`），即只有 **24 bit 熵（约 1677 万种）**。虽然不算极易爆破，但对公网（该接口经 Caddy 暴露在 `yolobubble.physmeta.cn`）而言，规律性前缀 `cad-日期-时间-` 让攻击者可枚举时间窗口内所有 job_id，逐个探测。
- 对比：`_handle_result_get`（取回结果）已经意识到这点，加了 `UPLOAD_TOKEN` 校验（`server.py:472`）。但 `_handle_status` 漏了。
- 虽然状态本身不含图纸内容，但**泄露任务存在性 + 文件名 + 错误信息 + 时间戳**，且与"图纸不出内网"红线的边界要求不一致。
- **修复**：`_handle_status` 与 `_handle_result_get` 一致，加 `UPLOAD_TOKEN` 校验（上传方/前端走 SSO 时另行处理，但至少要挡住匿名公网访问）。

### 🔴 S2. 回传结果接口无 size 上限，可被耗尽磁盘

`_handle_result`（`server.py:398-433`）读取 body 时**没有大小上限检查**。对比 `_handle_upload` 有 `MAX_UPLOAD_BYTES`（50MB）拦截，但结果回传没有。

- 任何持有合法 `NODE_TOKEN` 的节点（或泄露了 token 的人）可以无限上传，直到撑爆 `INFERENCE_STORAGE` 磁盘。
- 由于结果本来就是 zip，正常应在 1~10MB 量级，设 50MB 上限足够且不误伤。
- **修复**：在 `_handle_result` 开头加 `Content-Length` 上限检查（复用 `MAX_UPLOAD_BYTES` 或独立 `MAX_RESULT_BYTES`）。

### 🟡 S3. 结果回传未校验任务当前状态

`_handle_result` / `_handle_fail` / `_handle_progress` 只校验了 run_token 匹配，**没有校验任务状态必须为 `claimed` 或 `processing`**。

- 后果：一个已完成（`done`）的任务，如果 run_token 被重放，可以覆盖 `result_path` 写入新的 zip（状态又被改回 `done`，但内容被替换）。
- run_token 是 claim 时生成、随任务返回给节点的，正常节点不会重放，但这是"纵深防御"缺失。攻击面与 S2 类似（需先拿到 NODE_TOKEN + run_token）。
- **修复**：回传接口加 `WHERE status IN ('claimed','processing')` 的条件更新，或显式检查 `row["status"]`。

### 🟡 S4. `wait_seconds` 长轮询无并发保护（潜在 DoS）

`_handle_claim`（`server.py:338-367`）里 `while True: ... time.sleep(0.5)` 实现长轮询，最多挂起 30 秒。服务器是 `ThreadingHTTPServer`，每个连接占用一个线程。

- 若攻击者（或异常节点）并发发起大量 `claim`（每个挂 30 秒），会耗尽线程池。
- `ThreadingHTTPServer` 默认无线程上限，几十个并发长连接就够把服务拖慢（虽然 Python 线程有 GIL，sqlite 查询快，实际影响有限，但仍值得设防）。
- **修复**：可选——设 `wait_seconds` 更严格上限（已 30s 上限，尚可）、对 claim 做简单限流、或换成单线程 + 信号量。**低优先级**，视部署规模而定。

---

## 三、正确性 / 健壮性问题

### 🟡 C1. `_read_body` 可能读到不完整 body（已读一半）

`server.py:200-202` 的 `_read_body` 用 `self.rfile.read(length)` 一次性读。HTTP 上，客户端可能分片发送，`read(length)` 在 TCP 层不保证一次读满 `length` 字节。

- 对 `multipart/form-data` 大文件（几十 MB），实践中极可能读到不完整数据 → 图片/zip 损坏、SHA-256 校验失败（回传有校验会报错，上传没有 SHA 校验所以会静默存坏图）。
- **修复**：改成循环读满 `length` 字节（`while len(buf) < length: buf += rfile.read(length - len(buf))`）。`http.server` 的 `BaseHTTPRequestHandler` 其实有 `self.rfile.read(length)` 在多数实现里会阻塞到读满，但 CPython 的 socket `makefile` 行为并不保证，稳妥起见建议显式循环。

### 🟡 C2. VLM 的 `apply_chat_template(tokenize=False)` + `processor(text=..., images=...)` 用法与官方不一致

`vlm_fallback_windows.py:159-160`：

```python
text = processor.apply_chat_template(messages, tokenize=False, add_generation_prompt=True)
inputs = processor(text=text, images=crop, return_tensors="pt").to(_DEVICE)
```

官方 MiniCPM-V 4.6 推荐写法是 `processor.apply_chat_template(messages, tokenize=True, return_dict=True, return_tensors="pt")` 一次到位（见 HuggingFace 文档）。当前"先 tokenize=False 拿文本，再 `processor(text=..., images=...)`"是**旧版 VLM 的通用写法**，对 MiniCPM-V 4.6 的 `MiniCPM-V4_6Processor` 不一定正确——尤其该模型支持 `downsample_mode`（16x/4x）和 `enable_thinking`，这些参数在 `processor(text=...)` 路径下可能无法正确传递，导致视觉 token 处理行为与官方不一致。

- 注释里已经提到 `_MAX_EDGE=448` 是为规避 `slice 切分 patch 数不一致` 崩溃，说明作者在 resize 层面做过调优，但 chat template 这一层的兼容性需要**在 Windows 真机上验证**。
- **建议**：对照 `transformers 5.7.0` 官方 MiniCPM-V 4.6 示例改写成一次 `apply_chat_template(..., tokenize=True, return_tensors="pt")`，或至少实测当前写法在 5.7.0 下能正常出结果。

### 🟡 C3. `transformers>=5.7.0` 版本要求过于激进，且与文档注释自相矛盾

`requirements-windows.txt:17` 写 `transformers>=5.7.0`，而 `vlm_fallback_windows.py:42` 注释提到"transformers **5.17.0** 的 MiniCPM-V 4.6 在部分尺寸下...崩溃"。

- `>=5.7.0` 没有上界，会装到最新版（可能已到 5.17+），而注释里明确说 **5.17.0 有 bug**（source/slice patch 数不一致导致 reshape 崩溃）。
- 这意味着：按 requirements 装出来的环境，很可能**正好踩中注释里描述的崩溃**，除非恰好 pin 在一个已知稳定的版本。
- **修复**：把版本收敛为明确区间，例如 `transformers>=5.7.0,<5.17.0` 或干脆 pin 一个经过验证的精确版本（`==`）。这是最容易在客户 Windows 现场翻车的点。

---

## 四、文档 / 接口契约矛盾

### 🟡 D1. 上传入口到底是 `/upload` 还是 `/region`？两处文档打架

- `README.md` 和 `docs/上传入口改造方案.md`：明确写"复用 bubble-ocr 的 **`POST /upload`**"。
- `DEPLOY.md:21,39,137`：写的是"**`POST /region`** 时转发"、`_handle_region` 在配置了 `INFERENCE_QUEUE_URL` 时转发，并新增 `_forward_inference`/`_wait_inference`。

这二者是**不同的改造方案**：一个是"上传即入队"（/upload），一个是"点识别按钮时转发"（/region）。`docs/上传入口改造方案.md` 里其实自己承认了现状是"上传不触发推理，点识别按钮走 /region 才推理"——所以 **DEPLOY.md 的 `/region` 方案才与实际相符**，而 README 的 `/upload` 方案与现状描述自相矛盾。

- **影响**：接手部署的人（codeX 或其他 agent）可能按 README 去改 `/upload`，但真正要改的是 `/region`，导致白费功夫甚至改错。
- **修复**：统一为 `/region` 方案，README 的"四、接口契约"和"八、上传入口改造"两节需同步修正。

### 🟡 D2. 服务器接口列表与实际实现不一致（文档多了接口）

`DEPLOY.md:48-50` 列出的接口包括 `POST /api/inference/upload`，但 `server/server.py` 里 **upload 是 `POST`（对，`do_POST` 里有 `p == "/api/inference/upload"`）** —— 这个是一致的。但注意：

- `server/server.py` 的接口**没有** `POST /api/inference/upload` 之外的"上传"变体，而 `docs/上传入口改造方案.md` 里做法 A 的示例代码用的是 `urllib.request` 转发到 `/api/inference/upload`，一致 ✅。
- 真正的不一致：**`server.py` 里没有 `_handle_region`**（那是 bubble-ocr 仓库的代码，不在本仓库），DEPLOY.md 第 39-44 行描述的是**另一个仓库（bubble-ocr-mvp）的改动**，却写在本仓库的部署文档里。读者容易误以为这些改动在本仓库，实际上要 `git pull` 另一个私有仓库。

（这条不算 bug，是文档组织问题，但容易误导。）

### 🟡 D3. `server` 默认值三处不一致

- `node/inference-node.example.json:2`：`"server": "https://app.physmeta.cn"`
- `node/agent.py:295`：默认 `"https://app.physmeta.cn"`
- `DEPLOY.md:207` / README：节点实际要连的是 **`https://yolobubble.physmeta.cn`**

example 里的 `app.physmeta.cn` 是旧值/占位符，与线上真实域名 `yolobubble.physmeta.cn` 不一致。用户照着 example 填会连错服务。**建议 example 改成 `yolobubble.physmeta.cn` 或至少加注释说明。**

---

## 五、可维护性 / 小改进项（不紧急）

1. **`_handle_status` 与 `_handle_result_get` 的鉴权逻辑重复**：两处都要判断"任务不存在 / failed / done"，可以抽一个公共方法。
2. **`update_status` 用 f-string 拼 SQL**（`server.py:164`）：`fields` 的 key 目前都是代码内部硬编码（`result_path`、`error`），无注入风险，但 f-string 拼 SQL 是坏味道，建议白名单校验或参数化列名。
3. **`run_inference` 里 `sys.path.insert(0, ...)`**（`agent.py:506`）：在长驻进程里多次 insert 会累积重复路径（虽然无害，但脏）。建议用 `if path not in sys.path` 保护。
4. **`_reap_stale_claimed` 每次 claim 都执行**：虽然建了索引 `idx_jobs_status(status, created_at)`，但按 `updated_at` 过滤超时，没有 `(status, updated_at)` 索引，任务量大时每次 claim 全表扫 `claimed` 状态。建议加 `(status, updated_at)` 索引或低频化回收。
5. **`MONITOR_HTML` 内联了 100 行 HTML/JS**：可接受（单文件自包含），但若前端要改，维护不便。可选拆成 `monitor.html` 资源文件。
6. **`vlm_fallback_windows.py` 里 `read_all` 与 `fallback` 未复用**：`read_all` 和 `fallback` 有重复的遍历逻辑，可小重构。
7. **`.gitignore` 漏了 `*.safetensors`、`models/`**：`docs/MODEL_VERSION.md:39` 声称"`.gitignore` 排除 `*.safetensors`、`models/vlm/`、`models/adapters/`"，但实际 `.gitignore` 里**并没有这些规则**。这是一个文档与事实的偏差——虽然当前仓库里确实没有模型文件，但为了兑现"模型权重不入 git"的红线承诺，应把这几条补进 `.gitignore`。

---

## 六、修复优先级建议

| 优先级 | 项 | 工作量 | 风险 |
|---|---|---|---|
| P0 立即 | S1 状态接口加鉴权 | 5 行 | 低，改完要同步前端/上传方 |
| P0 立即 | S2 结果回传加 size 上限 | 3 行 | 低 |
| P0 立即 | C3 transformers 版本收敛 | 1 行 | 低，但需真机验证稳定版本 |
| P1 本周 | C2 VLM chat template 改官方写法 | 半小时 + 真机验证 | 中，可能影响识别效果 |
| P1 本周 | D1 统一上传入口方案（/region） | 文档 | 低 |
| P1 本周 | .gitignore 补模型排除规则 | 1 行 | 低 |
| P2 有空 | S3 状态机校验、S4 限流、C1 读满 body | 各几行 | 低 |

---

## 七、一句话总结

这套代码**架构干净、安全意识和降级容错都做得不错**，核心的"主动出站长轮询 + 双重校验 + 超时回收"是成熟的工业级设计。真正需要在**上线前**处理的，是三个"面子上看不到、但公网一挂就会被利用/翻车"的点：

1. **状态查询接口没鉴权**（S1）——匿名可枚举任务；
2. **结果回传没限流**（S2）——可被撑爆磁盘；
3. **transformers 版本没上界**（C3）——requirements 装出来的环境可能正好踩中注释里写的 5.17.0 崩溃 bug。

修掉这三点，其余按 P1/P2 排期即可。
